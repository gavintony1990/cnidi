server:
  port: ${CNIDI_USER_PORT:8085}

spring:
  application:
    name: cnidi-user
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_SERVER_ADDR:127.0.0.1:8848}

management:
  endpoints:
    web:
      exposure:
        include: health,info
